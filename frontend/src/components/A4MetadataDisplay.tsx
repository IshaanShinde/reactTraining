const A4MetadataDisplay = () => {
    return (
        <div className="component fill">
            Placeholder for the JSON output fetched from the form
        </div>
    );
};

export default A4MetadataDisplay;